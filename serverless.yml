service: dynamodb-lambda

plugins:
  - serverless-offline

provider:
  name: aws
  runtime: nodejs6.10
  region: eu-west-1
  timeout: 60

functions:
  api:
    handler: handler.api
    events:
      - http:
          path: api
          method: get
          cors: '*'

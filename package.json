{
  "name": "docker-lambda",
  "version": "1.0.0",
  "description": "## Requirements",
  "main": "handler.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "serverless offline start -s development --host 0.0.0.0 &",
    "stop": "pkill node || true"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "serverless-offline": "^3.18.0"
  }
}
